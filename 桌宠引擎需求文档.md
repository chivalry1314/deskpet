# 桌宠引擎项目需求文档

## 一、项目概述

做一个全本地运行的桌宠自定义软件，开源到 GitHub，用户无需注册/联网即可使用。

核心功能：用户上传自己的图片（小猫小狗等），配置动作状态，生成桌宠并在桌面运行。

---

## 二、技术选型

| 项目 | 选择 | 理由 |
|------|------|------|
| 框架 | **Tauri (Rust + Web前端)** | 跨平台、体积小(~5-15MB)、原生支持透明窗口 |
| 前端 | Vue / React | 开发快，编辑器界面用网页技术 |
| 打包 | GitHub Actions CI | 自动编译 Windows(.exe)/macOS(.app)/Linux(.AppImage) |
| 分发 | GitHub Release | 开源免费，无需代码签名 |
| 数据存储 | 本地文件系统 | 零服务器，零网络依赖 |

---

## 三、软件架构（全本地）

```
┌─────────────────────────────────────────────┐
│          DeskPet Engine (本地应用)            │
│  ┌──────────────┐    ┌──────────────────┐  │
│  │   宠物编辑器   │    │    宠物运行器      │  │
│  │  (创建/修改)  │ ←→ │   (桌面悬浮窗)     │  │
│  │              │    │                  │  │
│  │ • 上传图片    │    │ • 加载 .pet      │  │
│  │ • 配置状态    │    │ • 透明无边框窗口  │  │
│  │ • 预览调试    │    │ • 系统托盘常驻    │  │
│  │ • 导出 .pet  │    │ • 拖拽/点击交互   │  │
│  └──────────────┘    └──────────────────┘  │
│                                             │
│  数据存储: 本地 JSON + PNG 图片              │
│  无需服务器、无需账号、无需联网              │
└─────────────────────────────────────────────┘
```

---

## 四、.pet 文件格式

.pet 文件 = ZIP 压缩包（改后缀），内部结构：

```
橘猫小咪.pet
├── manifest.json          # 宠物配置文件
├── icon.png               # 列表展示图标
├── idle_01.png            # idle 状态帧1
├── idle_02.png            # idle 状态帧2
├── idle_03.png            # idle 状态帧3
├── clicked_01.png         # clicked 状态帧1
├── clicked_02.png         # clicked 状态帧2
├── clicked_03.png         # clicked 状态帧3
└── ...
```

### manifest.json 格式：

```json
{
  "name": "橘猫小咪",
  "version": "1.0",
  "author": "用户昵称",

  "window": {
    "width": 150,
    "height": 150,
    "scale": 1.0
  },

  "states": {
    "idle": {
      "frames": ["idle_01.png", "idle_02.png", "idle_03.png"],
      "fps": 8,
      "loop": true
    },
    "walk": {
      "frames": ["walk_01.png", "walk_02.png", "walk_03.png", "walk_04.png"],
      "fps": 12,
      "loop": true
    },
    "sleep": {
      "frames": ["sleep_01.png", "sleep_02.png"],
      "fps": 4,
      "loop": true
    },
    "clicked": {
      "frames": ["clicked_01.png", "clicked_02.png", "clicked_03.png"],
      "fps": 10,
      "loop": false,
      "next": "idle"
    }
  },

  "behavior": {
    "idle_time": [3, 10],
    "walk_speed": 2,
    "edge_bounce": true,
    "drag_physics": true
  },

  "interactions": {
    "on_click": "clicked",
    "on_drag": "drag",
    "on_hover": "blink"
  }
}
```

---

## 五、核心功能模块

### 5.1 宠物编辑器

- 宠物名称输入
- 图片上传区（支持拖拽）
- 状态管理（添加/删除/编辑状态）
- 每状态下：上传多帧图片、设置帧率(fps)、设置是否循环
- 行为配置：随机游走、边缘反弹、可被拖拽、点击反馈
- 实时预览窗口
- 导出 .pet 文件
- 保存到本地宠物库

### 5.2 宠物运行器（悬浮窗）

- **窗口特性**：
  - 无边框 (`decorations: false`)
  - 透明背景 (`transparent: true`)
  - 始终置顶 (`always_on_top: true`)
  - 不显示在任务栏 (`skip_taskbar: true`)
  - 鼠标穿透（非宠物区域点击穿透到桌面）

- **动画系统**：
  - 状态机管理（idle/walk/sleep/clicked 等）
  - 按 fps 轮播帧序列
  - 支持循环播放和单次播放
  - 单次播放结束后自动回到指定状态

- **交互系统**：
  - 鼠标拖拽移动宠物
  - 点击触发 clicked 状态动画
  - 悬停触发 hover 效果
  - 随机游走（自动在屏幕内移动）
  - 边缘碰撞反弹

- **系统托盘**：
  - 右键菜单：显示/隐藏宠物、设置、退出
  - 托盘图标常驻后台

### 5.3 宠物管理

- 本地宠物库列表展示
- 运行/停止宠物
- 编辑已有宠物
- 删除宠物
- 导入外部 .pet 文件
- 导出 .pet 文件分享

---

## 六、数据存储路径

```
Windows:  %APPDATA%/DeskPet/
macOS:    ~/Library/Application Support/DeskPet/
Linux:    ~/.config/DeskPet/

DeskPet/
├── pets/                    # 用户创建的宠物文件夹
│   ├── 橘猫小咪/
│   │   ├── manifest.json
│   │   └── *.png
│   └── 柯基阿黄/
│       └── ...
├── settings.json            # 全局设置
└── pets.json               # 宠物列表索引
```

---

## 七、状态机与点击响应机制

### 核心原理

用户上传的是**静态图片组**，点击响应的本质是**切换播放哪一组图片**。

### 状态机实现

```
状态列表:
├── idle    : 待机循环动画（默认状态）
├── walk    : 行走循环动画
├── sleep   : 睡觉循环动画
└── clicked : 被点击单次动画 → 自动回到 idle

点击流程:
用户点击宠物图片
    ↓
前端捕获 click 事件
    ↓
状态机从 "idle" 切换到 "clicked"
    ↓
按 clicked 的帧率和帧数组播放动画
    ↓
播完最后一帧（loop=false）
    ↓
自动切换回 "idle" 状态
```

### 兜底处理（用户图片不足时）

| 用户上传 | 引擎处理 |
|---------|---------|
| 只有1张 idle.png | 点击时：CSS 缩放抖动动画 |
| 有 idle.png + click.png | 点击时：切到 click 图，0.5秒后切回 |
| 有多帧序列 | 正常播放帧动画 |

---

## 八、Rust (Tauri) 核心接口

```rust
// 加载 .pet 文件
#[tauri::command]
fn load_pet(path: String) -> Result<PetConfig, String>

// 保存 .pet 文件
#[tauri::command]
fn save_pet(config: PetConfig, images: Vec<Vec<u8>>) -> Result<String, String>

// 创建宠物悬浮窗口
#[tauri::command]
fn spawn_pet_window(app: AppHandle, pet_path: String)

// 关闭宠物窗口
#[tauri::command]
fn close_pet_window(window_label: String)

// 获取本地宠物列表
#[tauri::command]
fn list_local_pets() -> Result<Vec<PetInfo>, String>

// 删除本地宠物
#[tauri::command]
fn delete_pet(pet_name: String) -> Result<(), String>
```

---

## 九、窗口配置（Tauri）

### 编辑器窗口（普通窗口）

```json
{
  "width": 900,
  "height": 650,
  "decorations": true,
  "transparent": false,
  "resizable": true,
  "center": true
}
```

### 宠物悬浮窗口（透明无边框）

```json
{
  "width": 150,
  "height": 150,
  "decorations": false,
  "transparent": true,
  "alwaysOnTop": true,
  "skipTaskbar": true,
  "resizable": false,
  "focus": false
}
```

---

## 十、GitHub 仓库结构

```
deskpet-engine/
├── src/                        # Rust 后端源码
│   ├── main.rs                 # 入口
│   ├── commands/               # Tauri 命令
│   │   ├── pet.rs              # 宠物 CRUD
│   │   └── window.rs           # 窗口管理
│   ├── pet/
│   │   ├── loader.rs           # .pet 文件解析
│   │   ├── packer.rs           # .pet 文件打包
│   │   ├── state_machine.rs    # 状态机
│   │   └── animator.rs         # 动画逻辑
│   ├── window/
│   │   ├── editor.rs           # 编辑器窗口
│   │   └── pet_viewer.rs       # 宠物悬浮窗
│   └── tray.rs                 # 系统托盘
│
├── src-ui/                     # 前端源码 (Vue/React)
│   ├── editor/                 # 编辑器界面
│   │   ├── App.vue
│   │   ├── components/
│   │   │   ├── PetCreator.vue
│   │   │   ├── StateManager.vue
│   │   │   ├── FrameUploader.vue
│   │   │   └── PreviewWindow.vue
│   │   └── stores/
│   │       └── petStore.js
│   │
│   └── viewer/                 # 宠物渲染页面
│       ├── pet-viewer.html
│       └── pet-viewer.js
│
├── examples/                   # 示例宠物
│   └── demo-cat.pet
│
├── .github/
│   └── workflows/
│       └── release.yml         # CI 自动打包
│
├── tauri.conf.json             # Tauri 配置
├── Cargo.toml                  # Rust 依赖
├── package.json                # 前端依赖
├── README.md
└── LICENSE (MIT)
```

---

## 十一、CI 自动打包 (GitHub Actions)

```yaml
# .github/workflows/release.yml
name: Release
on:
  push:
    tags: ['v*']

jobs:
  build:
    strategy:
      matrix:
        platform: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: __VERSION__
          releaseName: "DeskPet Engine __VERSION__"
```

---

## 十二、MVP 功能清单

### 第一阶段（核心可用）

- [ ] 编辑器：上传图片、创建状态、设置帧率
- [ ] 编辑器：预览动画效果
- [ ] 编辑器：导出 .pet 文件
- [ ] 运行器：加载 .pet 并显示透明悬浮窗
- [ ] 运行器：idle 状态循环动画
- [ ] 运行器：点击切换 clicked 状态，结束后回 idle
- [ ] 运行器：鼠标拖拽移动
- [ ] 运行器：系统托盘常驻
- [ ] 宠物管理：本地列表、运行/停止

### 第二阶段（体验优化）

- [ ] 随机游走 + 边缘反弹
- [ ] 多宠物同时运行
- [ ] 开机自启
- [ ] 音效支持
- [ ] 导入/导出 .pet 分享

### 第三阶段（生态）

- [ ] 皮肤市场/社区
- [ ] 更多交互类型
- [ ] 简单脚本支持（Lua）

---

## 十三、用户安装流程

```
Windows:
1. GitHub Release 下载 .exe 安装包
2. 双击安装（SmartScreen 点"更多信息"→"仍要运行"）
3. 打开软件 → 创建宠物 / 运行已有宠物

macOS:
1. GitHub Release 下载 .dmg
2. 拖拽到 Applications
3. 首次运行：右键 → 打开（或终端 xattr -cr）

Linux:
1. GitHub Release 下载 .AppImage
2. chmod +x 后运行
```

---

## 十四、关键设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 全本地 vs 网页+本地 | **全本地** | 零服务器成本，零网络依赖，体验更直接 |
| 统一引擎 vs 每个宠物独立exe | **统一引擎** | 体积小、易更新、安全可控 |
| 骨骼动画 vs 帧序列 | **帧序列** | 门槛低，用户只需准备静态图 |
| 文件名约定 vs 编辑器配置 | **两者支持** | 高级用户用约定，普通用户用编辑器 |
| 开源协议 | **MIT** | 最宽松，鼓励社区贡献 |

---

## 十五、风险与应对

| 风险 | 应对 |
|------|------|
| Windows SmartScreen 拦截 | 开源可审计，用户手动允许 |
| macOS Gatekeeper 拦截 | 用户右键打开或 xattr 解除 |
| 杀毒软件误报 | 代码完全开源，透明窗口行为可解释 |
| 用户上传版权图片 | 引擎不托管内容，用户自行负责 |
| 单张图无动画效果 | 提供 CSS 抖动等兜底效果 |

---

*文档生成时间: 2026-07-23*
*基于 Tauri 框架，目标平台: Windows / macOS / Linux*
